#!/usr/bin/env bash
# Homebrew tap 维护脚本（占位）— v0.5+ 落地。
#
# 计划工作：
# - 维护 homebrew-roundtable tap 仓库
# - 每次 release 后更新 formula 的 url + sha256
# - 提 homebrew-core PR（如果接受 core）
#
# Formula 名 TBD（候选 roundtable-cli / roundtable-ai / roundtable / 自维护 tap）。
#
# v0.1.0 暂不实装；用户用 npm install 即可。
set -e

echo "v0.1.0 暂不支持 Homebrew 安装；请用：npm install -g @roundtablelabs/cli"
echo "Homebrew tap 将在 v0.5+ 落地（详见 CHANGELOG 与 docs）"
exit 1
